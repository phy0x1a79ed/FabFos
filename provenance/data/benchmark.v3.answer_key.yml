# GENERATED by transforms/build/_stage/build_ref_library.py --hash
# Source of truth is provenance/data/_declared.yml. Do not hand-edit.
id: benchmark.v3.answer_key
src: null
status: MISSING
dest: validation/benchmark/v3/Y
type: ecspr::benchmark_answer_key
tier: validation
kind: derived
producer: transforms/build/benchmark/ (v3 fork of 20_build_panel.py + 21_build_Y.py)
inputs:
- benchmark.v3.ground_truth
- benchmark.v3.observations
- benchmark.v3.decisions
determinism: reproducible
degraded_mode: 'Without this, v3 has observations but no expectations, so the scorer has nothing
  to score against: directional specificity and the essentiality diagnostic both return empty
  and no validation result can be produced. The v1 contract staged at contract/ still documents
  the required shape, and v1 remains fully scorable, so the failure is a refusal rather than
  a wrong number.'
note: DECLARED BEFORE IT EXISTS, deliberately. The gap is then visible in the provenance gate
  now, rather than discovered at score time as a confusing empty join. Filled by the v3 answer-key
  build; frozen and hashed BEFORE any solve runs, which is the property that makes it an answer
  key at all.
sha256: null
reason: declared MISSING
